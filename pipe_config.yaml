pipe_struct:
  data:
    loaders:
      Load data to disk
    preprocessors:
      Preprocess data 
    post_loaders:
      Load new data with reduced initial data
  features:
    extractors:
      Extract features from data
    transformers:
      Transform features
