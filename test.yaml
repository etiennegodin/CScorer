config:
  yo: sckos
modules:
  data: !PipelineModule
    name: data
    status: !StepStatus 'init'
    steps:
      loader: !PipelineStep
        module: data
        name: loader
        status: !StepStatus 'init'
        substeps:
          gbif: !PipelineSubstep
            config: {}
            module: data
            name: gbif
            output: {}
            status: !StepStatus 'init'
            step: loader
      preprocessor: !PipelineStep
        module: data
        name: preprocessor
        status: !StepStatus 'init'
        substeps: {}
  features: !PipelineModule
    name: features
    status: !StepStatus 'init'
    steps: {}
name: my_pipeline
